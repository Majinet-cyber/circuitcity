╔══════════════════════════════════════════════════════════════════════════════╗
║                 PYTEST + CYPRESS INTEGRATION - QUICK REFERENCE                ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🚀 PRIMARY COMMANDS                                                           │
└──────────────────────────────────────────────────────────────────────────────┘

  npm run test:all          ← SSOT: Run ALL tests (pytest → cypress)
  npm run pytest:all        ← Run only PyTests with reports
  npx cypress run           ← Run Cypress (pytest hook runs first)
  node scripts/verify_test_system.mjs  ← Verify system integrity

┌──────────────────────────────────────────────────────────────────────────────┐
│ 📊 PYTEST COMMAND (Exact)                                                     │
└──────────────────────────────────────────────────────────────────────────────┘

  python -m pytest --junitxml=reports/pytest/junit.xml --tb=short -v

  ✅ NO --maxfail (runs ALL tests)
  ✅ Produces JUnit XML
  ✅ Verbose output
  ✅ Short traceback

┌──────────────────────────────────────────────────────────────────────────────┐
│ 📁 ARTIFACTS GENERATED                                                        │
└──────────────────────────────────────────────────────────────────────────────┘

  reports/pytest/junit.xml      JUnit format test results (CI-ready)
  reports/pytest/output.txt     Raw pytest console output (full)
  reports/pytest/summary.md     Human-readable failure summary

  Note: reports/ is gitignored (generated at runtime)

┌──────────────────────────────────────────────────────────────────────────────┐
│ 📝 SUMMARY.MD FORMAT                                                          │
└──────────────────────────────────────────────────────────────────────────────┘

  # PyTest Execution Summary
  
  ## Test Results
  - Total, Passed, Failed, Errors, Skipped counts
  
  ## ❌ Test Failures
  
  ### Failure Categories
  - Grouped by error type (AssertionError, AttributeError, etc.)
  
  ### Detailed Failures
  For each failure:
    - Test case name + classname
    - File path
    - Error type
    - Error message (first 30 lines max)

  ## Artifacts
  - Links to junit.xml, output.txt, summary.md

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🔄 EXECUTION FLOW                                                             │
└──────────────────────────────────────────────────────────────────────────────┘

  npm run test:all
    │
    ├─► run_pytests_and_summarize.mjs
    │   ├─► python -m pytest ...
    │   ├─► Generate junit.xml
    │   ├─► Generate output.txt
    │   ├─► Generate summary.md
    │   └─► Exit 0 (always, to allow Cypress)
    │
    ├─► Parse junit.xml to check for failures
    │
    ├─► npx cypress run
    │   └─► before:run hook (runs pytest again)
    │
    └─► Exit 1 if EITHER pytest OR cypress failed

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🧪 ERROR CATEGORIES (Auto-detected)                                           │
└──────────────────────────────────────────────────────────────────────────────┘

  NoReverseMatch          Django URL routing issues
  TemplateSyntaxError     Django template errors
  NameError               Undefined variables
  AssertionError          Test assertion failures
  AttributeError          Missing attributes/methods
  KeyError                Missing dictionary keys
  TypeError               Type mismatches
  ValueError              Invalid values
  ImportError             Module import failures
  PermissionError         403 errors
  NotFound                404 errors
  ServerError             500 errors
  Other                   Uncategorized errors

┌──────────────────────────────────────────────────────────────────────────────┐
│ 📦 FILES CREATED/MODIFIED                                                     │
└──────────────────────────────────────────────────────────────────────────────┘

  Created:
    ✅ scripts/run_pytests_and_summarize.mjs  (316 lines)
    ✅ scripts/run_all_tests.mjs              (110 lines)
    ✅ scripts/verify_test_system.mjs         (150 lines)
    ✅ scripts/README_TEST_SYSTEM.md          (270 lines)
    ✅ IMPLEMENTATION_SUMMARY.md              (Full documentation)

  Modified:
    ✅ cypress.config.js      (Added before:run hook)
    ✅ package.json           (Added pytest:all, test:all scripts)
    ✅ .gitignore             (Added reports/ directory)

┌──────────────────────────────────────────────────────────────────────────────┐
│ ✅ CONSTRAINTS MET                                                            │
└──────────────────────────────────────────────────────────────────────────────┘

  ✅ No regressions to existing test setup
  ✅ Works on Windows (PowerShell) and CI
  ✅ Runs ALL tests (no --maxfail limiting)
  ✅ Readable summary of failures
  ✅ Always generates reports (even on failure)
  ✅ Does NOT fix any tests (only reports)

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🔒 LOCKED IN AS SSOT                                                          │
└──────────────────────────────────────────────────────────────────────────────┘

  This implementation is the Single Source of Truth for:
    1. Test execution order (PyTest → Cypress)
    2. Report generation (3 artifacts)
    3. Failure handling (never stops early)
    4. CI/Local parity (same commands)
    5. Windows/Linux compatibility

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🎯 CI INTEGRATION EXAMPLE                                                     │
└──────────────────────────────────────────────────────────────────────────────┘

  - name: Run All Tests
    run: npm run test:all

  - name: Upload Test Reports
    if: always()
    uses: actions/upload-artifact@v3
    with:
      name: test-reports
      path: reports/

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🛡️ IMPORTANT NOTES                                                            │
└──────────────────────────────────────────────────────────────────────────────┘

  1. PyTest script ALWAYS exits 0 (allows Cypress to run)
  2. Wrapper script checks junit.xml for actual pytest status
  3. CI fails only if run_all_tests.mjs returns 1
  4. Error messages truncated to 30 lines in summary
  5. Full details always in output.txt
  6. Reports are auto-generated and gitignored

╔══════════════════════════════════════════════════════════════════════════════╗
║                           🎉 SYSTEM READY TO USE                              ║
╚══════════════════════════════════════════════════════════════════════════════╝

  Run: npm run test:all


{
  "name": "tengasale-e2e",
  "version": "1.0.0",
  "description": "TengaSale Cypress E2E tests",
  "private": true,
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:run:chrome": "cypress run --browser chrome",
    "test:e2e": "cypress run"
  },
  "devDependencies": {
    "cypress": "^13.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
